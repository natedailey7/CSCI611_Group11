**Build the index (once):**

python FinalProject_working/BM25/01_build_bm25_index.py

**Query:**

PowerShell recommended quoting: python FinalProject_working/BM25/02_query_bm25.py --query 'What is packet switching?'

Also works without quotes now: python FinalProject_working/BM25/02_query_bm25.py --query What is packet switching?

**Full dev retrieval + metrics:**

python 03_run_dev_retrieval.py --output data/SQuAD/bm25_topk_working.jsonl --report-recall

---

## RAG (BM25 + GPT-2)

This is a simple Retrieval-Augmented Generation (RAG) demo:
- Retriever: BM25 over SQuAD v2 dev paragraphs
- Generator: Hugging Face `gpt2` (not instruction-tuned)

**Install deps (once):**

pip install transformers torch

**Single query:**

python FinalProject_working/BM25/04_rag_gpt2.py --query "When was the Eiffel Tower built?"

**Run over dev (writes JSONL):**

python FinalProject_working/BM25/04_rag_gpt2.py --run-dev --limit 50 --output data/SQuAD/gpt2_rag_bm25.jsonl